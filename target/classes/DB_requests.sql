create table Person(
    person_id int GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    fullName varchar(100) UNIQUE,
    yearOfBirth int
);

create table Book(
    book_id int GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    person_id int REFERENCES Person(person_id),
    name varchar(100) NOT NULL,
    author varchar(100) NOT NULL,
    yearProduction int NOT NULL
);
SELECT * FROM Person;
SELECT * FROM Book;
INSERT INTO Person(fullname, yearofbirth) VALUES('Иванов Иван Иванович', '1980');
INSERT INTO Book(name, person_id, author, yearproduction) VALUES('Волшебная гора', 1, 'Томас Манн', 1924);
DROP TABLE Book